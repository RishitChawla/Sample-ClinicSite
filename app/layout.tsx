import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "HealthCare Clinic - Best Medical Care in Delhi, India",
  description:
    "Professional healthcare services in Delhi with experienced doctors, modern facilities, and comprehensive medical treatments. Book your appointment today.",
  keywords: "clinic Delhi, doctor Delhi, healthcare, medical services, health checkup, cardiology, pediatrics",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable} antialiased`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
